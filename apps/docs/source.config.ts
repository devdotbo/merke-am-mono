import { defineDocs, defineConfig } from "fumadocs-mdx/config";

// Define docs and meta collections for Fumadocs MDX (use defaults)
export const { docs, meta } = defineDocs({});

export default defineConfig({});


